import Head from 'next/head';

const Includes = () => (
    <Head>
        <title>Daniel Schmitz</title>
        <meta name="viewport" content="initial-scale=1.0, width=device-width"/>
    </Head>
);

export default Includes;